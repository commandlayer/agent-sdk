[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "commandlayer-agent-sdk"
version = "0.0.0"
description = "Early scaffold for CommandLayer Python SDK"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "CommandLayer" }]
